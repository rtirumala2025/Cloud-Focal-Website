import React from 'react';
import Footer from './Footer';

const ConditionalFooter = () => {
  // Dark mode removed: always use light Footer
  return <Footer />;
};

export default ConditionalFooter;
